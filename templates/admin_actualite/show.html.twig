<!DOCTYPE html>

<title>Actualite</title>

{% block body %}
    <h1>Actualite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actualite.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ actualite.description }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ actualite.lieu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_actualite_index') }}">back to list</a>

    <a href="{{ path('app_admin_actualite_edit', {'id': actualite.id}) }}">edit</a>

    {{ include('admin_actualite/_delete_form.html.twig') }}
{% endblock %}
