<!DOCTYPE html>

<title>Edit Property</title>

{% block body %}
    <h1>Edit Property</h1>

    {{ include('admin_property/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_admin_property_index') }}">back to list</a>

    {{ include('admin_property/_delete_form.html.twig') }}
{% endblock %}
