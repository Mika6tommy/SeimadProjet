<!DOCTYPE html>

<title>Province index</title>

{% block body %}
    <h1>Province index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Lieu</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for province in provinces %}
            <tr>
                <td>{{ province.id }}</td>
                <td>{{ province.lieu }}</td>
                <td>
                    <a href="{{ path('app_admin_province_show', {'id': province.id}) }}">show</a>
                    <a href="{{ path('app_admin_province_edit', {'id': province.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_province_new') }}">Create new</a>
{% endblock %}
